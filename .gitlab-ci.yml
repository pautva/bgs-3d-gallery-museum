image: node:18

pages:
  stage: deploy
  script:
    - npm ci
    - npm run build
    - mkdir .public
    - cp -r dist/. .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - main